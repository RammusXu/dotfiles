{{- if (eq .chezmoi.os "darwin") -}}
#!/bin/bash

brew bundle --no-lock --file=/dev/stdin <<EOF
brew "git"
brew "helm"
brew "k9s"
brew "jq"
brew "hey"
brew "httpie"
cask "google-chrome"
cask "iterm2"
cask "ngrok"
EOF
{{ end -}}
