{{- /* 身分完全由【目錄】決定，不看「這台是不是公司的電腦」——
       每台機器都可能同時放公司和個人的 repo。 */ -}}
[user]
    # 沒有預設身分。沒被下面 includeIf 涵蓋到的目錄裡 commit 會直接報錯，
    # 而不是安靜地用錯 email 送出去。臨時 repo 就在那個 repo 裡自己
    # `git config user.email ...`，這是刻意的摩擦。
    useConfigOnly = true

# 一條規則對一個目錄，沒有例外：
#   ~/workspace/ → 公司      ~/personal/ → 個人
# dotfiles 本身就住在 ~/personal/dotfiles，所以不需要任何 includeIf 例外。
#
# git 的 includeIf 是「後者覆蓋前者」。真的有個人專案不得不放在 ~/workspace/
# 底下時，在 work 那一行【後面】補一行指向 .gitconfig-personal 即可。
[includeIf "gitdir:{{ .chezmoi.homeDir }}/workspace/"]
    path = {{ .chezmoi.homeDir }}/.gitconfig-work
[includeIf "gitdir:{{ .chezmoi.homeDir }}/personal/"]
    path = {{ .chezmoi.homeDir }}/.gitconfig-personal

[credential]
    helper = osxkeychain
[pager]
    branch = false
[core]
    excludesfile = {{ .chezmoi.homeDir }}/.gitignore_global
[init]
    defaultBranch = main
[push]
    autoSetupRemote = true
[fetch]
    prune = true
