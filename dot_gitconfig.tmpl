{{- /* 身分由「目錄」決定，不靠 hostname、也不靠記憶手動切。 */ -}}
[user]
    # 沒有預設身分。在沒被下面 includeIf 覆蓋到的目錄裡 commit 會直接報錯，
    # 而不是安靜地用錯的 email 送出去 —— 這正是我們要的行為。
    useConfigOnly = true

[includeIf "gitdir:{{ .chezmoi.homeDir }}/workspace/"]
    path = {{ .chezmoi.homeDir }}/.gitconfig-work
[includeIf "gitdir:{{ .chezmoi.homeDir }}/personal/"]
    path = {{ .chezmoi.homeDir }}/.gitconfig-personal
[includeIf "gitdir:{{ .chezmoi.homeDir }}/.local/share/chezmoi/"]
    path = {{ .chezmoi.homeDir }}/.gitconfig-personal

[credential]
    helper = osxkeychain
[pager]
    branch = false
[core]
    excludesfile = {{ .chezmoi.homeDir }}/.gitignore_global
[init]
    defaultBranch = main
[push]
    autoSetupRemote = true
[fetch]
    prune = true
