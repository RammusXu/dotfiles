{{- $role := "primary" -}}{{- if hasKey . "role" -}}{{- $role = .role -}}{{- end -}}
# --- repo-only files: 絕對不要 apply 到 $HOME ---
README.md
Makefile
LICENSE
scripts
docs
.githooks
.ruby-lsp
*.tar.gz

# ~/.zshenv 是 secrets，永遠由 Bitwarden 還原，不該被 chezmoi 碰
# （這行也擋掉手滑 `chezmoi add ~/.zshenv`）
.zshenv

# Brewfile 由 run_onchange_after_10-brew.sh 讀取，不是要放進 $HOME 的檔案
Brewfile
Brewfile.gui
Brewfile.manual
Brewfile.unmanaged

{{ if ne $role "primary" }}
# runner（背景機）不需要 GUI 相關設定
.p10k.zsh
{{ end }}
